import React from 'react'
import {AppBar, Toolbar} from '@material-ui/core'

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        Header
      </Toolbar>
    </AppBar>
  )
}

export default Header
